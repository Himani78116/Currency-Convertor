# Currency-Convertor
Created a Currency Convertor using API's in JavaScript.
The script.js,code.js and style.css files are linked with index.html file.
The file code.js contains an object named countryList which carries the abbreviation of the country names worldwide which is required to identify or convert the currency.
For icons, external css file(fontawesome) is linked to html file.


